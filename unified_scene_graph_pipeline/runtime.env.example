# Copy to a server-only file outside version control, chmod 600, and fill values.
INFERENCE_API_KEY=
HF_TOKEN=
DATASET_ROOT=/datasets
RUN_ROOT=/datasets/unified_scene_graph_runs
MODEL_ROOT=/datasets/unified_scene_graph_models
